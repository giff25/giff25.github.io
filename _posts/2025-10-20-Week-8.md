## Week 8: Performing gradient descent on the exponential function.

### The plan: 

I need to apply everything we’ve done so far to the exponential function. Because the code was becoming cumbersome and unorganised, I needed to spend some time refactoring. I will make a new file that is solely for the exponential equation; furthermore, I will create a dedicated cell for any helper functions that I can extract from the code. The thought of making this a dedicated class crossed my mind, but refactoring explicitly for this purpose may take too much time; therefore, I'll start with small changes, and if the opportunity arises, take it. 
	Applying gradient descent to the exponential function shouldn't be too different from what I currently have in the code. The main difference is having to watch 3 parameters, one of which is a vector. 

### Implementation:
	
	While refactoring, I noticed a lack of comments. This led me to separate the code into small chunks by relation and comment on their purpose. This helped me solidify my understanding and reduce clutter in the code. After refactoring, I added the changes necessary to perform gradient descent on the exponential function: 
with tf.GradientTape(persistent=True) as tape:
    tape.watch([a, b, m])
    y = exponential_2d
  [da, db, dm] = tape.gradient(y, [a, b, m])


  a.assign_sub(learning_rate * da)
  b.assign_sub(learning_rate * db)
  m.assign_sub(learning_rate * dm)
Following this change, I encountered multiple errors. The first major one was an issue with the gradient descent outputting Nan values. After doing some digging, I discovered that TensorFlow doesn't track gradient descent through external libraries. This was a problem because my exponential function was using Numpy for its calculations. I found suitable replacements under the TensorFlow math library. After making these adjustments, more errors were generated from having unexpected return types from the TensorFlow functions. I did a few hacks to get the returns I needed and got the entire thing working. Unfortunately, my loss function started outputting negative values. At this point, I decided to use my time more efficiently and ask Professor Bernardo for help. 
