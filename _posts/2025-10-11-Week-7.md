## Week 7: gradient descent in practice cont.


### The plan:
Following the successful implementation of gradient descent last week, I will apply it to a multivariate polynomial. After which, I will use the exponential function.
### Implementation:
First, changing the polynomial to include an additional variable, “a”, results in:
```python
def l(x):
  return 2 * x**6 + 2 * x**5 - 2 * x**4 - x**3 + 2 + 2 * a**2
```
The GradientTape module documentation indicates that I need to run tape.gradient() with respect to different variables; hence, the calls will be tape.gradient(y, x)and tape.gradient(y, a). Making the necessary changes to the code changed the graph to the following:
<img width="512" height="393" alt="gradient3" src="https://github.com/user-attachments/assets/b1ae4b4b-83ec-440d-a69f-cf2e9c07ba1c" />

