# Week 9:  Performing gradient descent on the exponential function cont.

### The plan: 

Considering the clarification on the gradient descent's role, I need to adjust my code so that it combines the exponential function's values during its forward step. I will refactor the code further to have a dedicated loss function that includes this entire step.

### The implementation:
	Since I had the library-specific errors out of the way, things went smoothly. I refactored the code into a dedicated loss function: 
```python
def loss(a,b,m,z, XYarray):
  f = tf.constant(0.0)
  for each_Piezos_XY in XYarray:
    f += exponential_2d(a, b, m)(each_Piezos_XY)
  return (f-z)**2
```

After testing this code, I ended up with outputs closer to what I was expecting:
<img width="512" height="282" alt="gradient4" src="https://github.com/user-attachments/assets/2b1420a1-ee64-4af5-acca-55f1aad6aec0" />

The difference between our function output and the combined piezo values was approaching a negligible difference; however, when I compared the final mbar value (under this context, it's the location where the “hit” likely occurred) with the piezo graph I had before, it didn't look right. The next logical step will be to verify the algorithm's correctness; however, I'd like to get Professor Prado’s opinion before I set out to do this time-consuming step. While I wait for our meeting, I will move on to the hardware portion of this project.
